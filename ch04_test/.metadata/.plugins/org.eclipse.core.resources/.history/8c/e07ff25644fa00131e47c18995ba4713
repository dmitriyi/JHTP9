package ch04_test;

import java.awt.Graphics;
import java.applet.Applet;

import javax.swing.JPanel;

public class DrawPanel extends JPanel{
	public void paintComponent (Graphics g) {
		super.paintComponent(g);
		
		int width = getWidth();
		int height = getHeight();
		int iWidth	= width;
		int iHeight = height;
		int i1LineCounter = 1;
		int iX = 400;
		int iY = 400;
		
		while (i1LineCounter <= 15){
			g.drawLine(0, 0, iX, iY);
			iX = iX - 26;
			iY = iY-26;
//			iX = (width - (i1LineCounter - 1)) *(width /15);
//			iY = (height - (i1LineCounter - 1)) * (height/15);
//			iWidth = (iWidth + (i1LineCounter - (i1LineCounter - 1) )) * 26;
//			iHeight = (iHeight - (i1LineCounter - (i1LineCounter - 1) )) * 26;
			i1LineCounter +=1;
		}
		
		
		
//		int i3LineCounter = 1;
//		while (i3LineCounter <= 15){
//			g.drawLine(0, iWidth, iHeight, 0);
//			i3LineCounter +=1;
//			iWidth -= 26;
//			iHeight += 26;    
//		}
		
//		g.drawLine(0, height, width, 0);
	}
	
}
