package ch3_Grade;

import org.omg.CORBA.PUBLIC_MEMBER;

public class Account {

	private double balance;
	
	public Account (double initBalance){
		
		if (initBalance > 0.0){
			balance = initBalance;
		}
		
		public void cred ( double ammount ) {
			balance = balance + ammount;
		}
	}
	
}
