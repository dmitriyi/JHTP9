package ch2_Me_Print;
// Package that all of this program belongs to

public class OutPrint3 {
	//Class Declaration - Named Out Print
	
	public static void main (String[] args){
		//One of the methods in class must be called main!!!
	// Void means - this method will not return information\
		String test = new String("test");
		String str = String.format("String: %s%s", test, test);
		
	System.out.printf("%s", "Hello! And welcome"/*, "to Java Programming!"*/ );
	// TODO Figure Out a pfrintf method so it would compile
	
	}

}
